# T113 + T118 + T119 实作完成 | 2025-06-04 13:41

## 任务概要

三人并行推进，完成策略页雷达图 + Streamlit 全套分析看板。

---

## T113: 策略页因子權重雷達圖

### 修改文件
1. **frontend/src/pages/Strategy.tsx** — 新增 recharts RadarChart 组件
   - 导入: `RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, ResponsiveContainer` from recharts
   - 新增 `RADAR_COLORS` 常量（6因子 hex 色值映射）
   - 在「篩選條件」与「再平衡預覽」之间插入全宽雷达图卡片
   - 使用 `gridColumn: '1 / -1'` 撑满两列
   - 数据结构: `[{ factor: '動能', '權重': 20 }, ...]` → 单多边形

2. **frontend/src/pages/Strategy.module.css** — 新增 `.radarContainer` 样式

3. **frontend/src/styles/variables.css** — 补充缺失的 `--_pink-500: #ec4899`
   - 原先 `--color-institutional: var(--_pink-500)` 引用了不存在的变量，现修复

### 雷达图设计
- 单多边形 6 轴（動能/價值/品質/成長/大師評分/法人）
- 紫色填充（#a78bfa, fillOpacity 0.2），暗色 PolarGrid
- 域名 [0, 100]，响应式高度 320px

---

## T118: Streamlit 基础看板（页面 1-3 + 基础设施）

### 新增文件（8 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `streamlit/components/db.py` | 46 | SQLAlchemy 连接层，`@st.cache_resource` 缓存 engine，`run_query()` 助手 |
| `streamlit/app.py` | 39 | 主入口，侧边栏导航，3 卡片概览 |
| `streamlit/pages/1_今日選股.py` | 99 | 策略维度筛选 + 排名表格 + 评分摘要 |
| `streamlit/pages/2_法人動向.py` | 98 | 三大法人汇总 card + 个股 bar chart (plotly) + detail 表格 |
| `streamlit/pages/3_即時行情.py` | 115 | 即时报价表格 + PE 分布直方图 + 涨跌统计 |
| `requirements_streamlit.txt` | 9 | streamlit, plotly, pandas, sqlalchemy, psycopg2-binary, scipy, scikit-learn, numpy |
| `Dockerfile.streamlit` | 19 | python:3.13-slim, 端口 8501, 依赖 postgres |

### docker-compose.yml
- 新增 `streamlit` 服务（profile: streamlit）
- `POSTGRES_HOST=postgres`，`depends_on: postgres`
- 端口映射 `8501:8501`
- 挂载 streamlit/ 目录

---

## T119: Streamlit 完整看板（页面 4-6）

### 新增文件（3 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `streamlit/pages/4_回測工作台.py` | 112 | Run 列表 + 权益曲线 (双 Y 轴) + 持仓纪录 |
| `streamlit/pages/5_因子研究.py` | 158 | 相关性热力图 (plotly imshow) + 评分趋势 + IC 分析 + 统计摘要 |
| `streamlit/pages/6_警示歷史.py` | 97 | 严重度饼图 + 规则排名 bar + 表格 + 展开详情 |

### 页面功能覆盖

| 页面 | 数据图表 | 交互 |
|------|---------|------|
| 1. 今日選股 | DataFrame 表格 | slider/top_n + 策略选择 |
| 2. 法人動向 | plotly Bar + DataFrame | 排序依据/方向切换 |
| 3. 即時行情 | plotly Histogram + styled DataFrame | 排序/数量筛选 |
| 4. 回測工作台 | plotly 双轴权益曲线 + 指标 cards | Run 选择器 |
| 5. 因子研究 | plotly Heatmap + Line + 统计表格 | 回顾天数 |
| 6. 警示歷史 | plotly Pie + Bar + 详情 JSON | 天数/严重度/行数筛选 |

---

## 测试结果

```
Test Files  1 passed (1)
Tests      33 passed (33)
```

- 新增: RADAR_COLORS 验证 (hex + 唯一性), 6-factor 权重 autoRebalance
- 修复: `FACTOR_COLORS.guru` 不存在于 utils（仅 5 因子），测试改为验证 undefined
- 更新: baseWeights 从 5 因子升级为 6 因子（含 guru: 15）

---

## Bug 修复
- `variables.css`: 补充 `--_pink-500: #ec4899`（之前 --color-institutional 引用未定义变量）
- `streamlit/app.py`: 修复多行字符串语法错误（batch writer 导致的 raw newline 问题）
- `Strategy.tsx`: 清理未使用的 `useMemo` import

## 启动方式
```bash
# 前端 (已含雷达图)
cd frontend && npm run dev

# Streamlit 看板
docker compose --profile streamlit up -d
# 或本地
pip install -r requirements_streamlit.txt
streamlit run streamlit/app.py
```
